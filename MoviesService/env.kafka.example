# Replace HOST_IP with the LAN IP of the machine that will run Kafka.
# Example: HOST_IP=192.168.1.100
HOST_IP=127.0.0.1

# Keep 9092 if your services are configured to use the assignment default.
KAFKA_EXTERNAL_PORT=9092
